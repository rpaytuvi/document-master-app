import {Metrics} from '../../../themes';

const Styles = {
  container: {
    flex: 1,
    flexDirection: 'row',
    marginBottom: 2 * Metrics.base,
    paddingHorizontal: 2 * Metrics.base,
    justifyContent: 'space-between',
    flexWrap: 'wrap',
  },
  buttonGroupContainer: {
    width: 140,
  },
};

export default Styles;
