import Metrics from './metrics';

const size = {
  title: 3 * Metrics.base,
  large: 2.5 * Metrics.base,
  regular: 2 * Metrics.base,
};

const weight = {
  bolder: '700',
  bold: '600',
  regular: '400',
};

export default {
  size,
  weight,
};
